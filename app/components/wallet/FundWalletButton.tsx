"use client";

// Re-enabled in Phase 3.4 once the Minitia rollup is live. Stubbed for now
// so import sites continue to compile while InterwovenKit is deferred.

export default function FundWalletButton(_props: {
  className?: string;
  label?: string;
  variant?: "primary" | "ghost";
}) {
  return null;
}
